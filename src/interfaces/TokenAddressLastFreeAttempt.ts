export interface TokenLastFreeAttempt {
    TokenAddress: string; // Indica l'indirizzo del token
    LastFreeAttempt: Date; // Indica l'ultima volta in cui è stata fatta un'operazione gratuita per il token
}